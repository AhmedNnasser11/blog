import styled from "styled-components"

export const AccessibilityContainer = styled.div``

export const UList = styled.ol`
    list-style: none;
   counter-reset: item;
   padding: 0;
   margin: 0;
`